Show advancing status and next actions.

## Steps

1. Run `./bandlab-cli advance`
2. Summarize: how many shows need outreach, how many awaiting reply, how many confirmed
3. If `$ARGUMENTS` contains a show ID or partial, run `./bandlab-cli advance-contacts $ARGUMENTS` for that specific show
